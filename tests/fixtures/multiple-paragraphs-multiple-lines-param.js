/**
 * This is a description.
 * And this is more of it.
 *
 * @param {Boolean} isAdditive -  Set to true to allow the dropdown to add in custom values.
 * When `dropdownMode` is set to false, `isAdditive` is always true.
 *
 * This is a second paragraph
 * with two lines.
 */
